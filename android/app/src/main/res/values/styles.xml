<resources>
    <!-- Dark fintech chrome: kills the white launch flash and keeps the system bars
         matched to the dashboard background while the WebView loads. -->
    <style name="Theme.App" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">#0A1228</item>
        <item name="android:statusBarColor">#0A1228</item>
        <item name="android:navigationBarColor">#0A1228</item>
    </style>
</resources>