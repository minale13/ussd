node_modules/
dist/
.env
coverage/

# Android / Gradle build output
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/.idea/
android/captures/
*.apk
*.aab
*.keystore
*.jks
# The fixed signing key is committed on purpose so local and CI builds share
# one signature and an installed APK can be updated in place.
!android/app/keystore.jks

# OS / editor / temp
.DS_Store
Thumbs.db
*.log
.tmp-*

# Scratch launcher for the local UI preview server
do-prev.cmd
